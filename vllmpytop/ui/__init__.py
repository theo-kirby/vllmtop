"""TUI package: curses app, panels, layout engine, theme, and drawing widgets.

Submodules:
- :mod:`ui.app` — curses main loop and background poller
- :mod:`ui.panels` — per-panel draw functions (GPU, throughput, requests, perf)
- :mod:`ui.layout` — panel rectangle computation with reflow on resize
- :mod:`ui.theme` — btop color palette, gradients, and threshold helpers
- :mod:`ui.widgets` — braille charts, meters, bars, and formatting utilities
"""
